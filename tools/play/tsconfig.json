{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "declaration": false,
    "declarationMap": false,
    "sourceMap": false,
    // A CLI, so unlike the engine it legitimately wants Node's globals.
    "types": ["node"]
  },
  "include": ["src/**/*.ts"]
}
